const names={RISE:"subida",FALL:"bajada",EVEN:"par",ODD:"impar",OVER:"mayor",UNDER:"menor"};
class Voice{constructor(){this.enabled=true;this.voices=[];this.voice=null;this.rate=.92}async init(){if(!("speechSynthesis"in window)){this.enabled=false;return}await new Promise(r=>setTimeout(r,250));this.voices=speechSynthesis.getVoices();this.voice=this.voices.find(v=>v.lang.toLowerCase()==="es-sv")||this.voices.find(v=>v.lang.startsWith("es"))||this.voices[0]}speak(t){if(!this.enabled||!t)return;speechSynthesis.cancel();let u=new SpeechSynthesisUtterance(t);if(this.voice){u.voice=this.voice;u.lang=this.voice.lang}else u.lang="es-SV";u.rate=this.rate;speechSynthesis.speak(u)}toggle(){this.enabled=!this.enabled;if(!this.enabled)speechSynthesis.cancel();return this.enabled}select(id){this.voice=this.voices.find(v=>`${v.name}|${v.lang}`===id)||this.voice}prepare(o){this.speak(o.direction==="MATCH"?`Atención. Posible coincidencia ${o.metadata?.digit}. Prepare el bot.`:`Atención. Posible ${names[o.direction]}. Prepare el bot.`)}revalidate(o){this.speak(o.direction==="MATCH"?`Revalidando coincidencia ${o.metadata?.digit}.`:`Revalidando ${names[o.direction]}.`)}confirm(o){this.speak(o.direction==="MATCH"?`Coincidencia ${o.metadata?.digit} confirmada. Ejecute ahora.`:`${names[o.direction]} confirmada. Ejecute ahora.`)}cancel(){this.speak("Oportunidad cancelada. Continuando la búsqueda.")}result(ok){this.speak(ok?"Resultado acertado. Buscando otra oportunidad.":"Resultado fallido. Buscando otra oportunidad.")}}
export const voice=new Voice();
